name: Collect Daily PCR Data

on:
  schedule:
    # Roda às 16:00 UTC (13:00 BRT) de segunda a sexta
    # Mercado aberto, dados de OI disponíveis no site
    - cron: '0 16 * * 1-5'
  
  # Permite execução manual
  workflow_dispatch:

jobs:
  collect-pcr:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install Chrome
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable
      
      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install pandas requests selenium webdriver-manager supabase yfinance
      
      - name: Run PCR collection
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
        run: python scripts/collect_pcr.py
      
      - name: Report status
        if: always()
        run: |
          echo "PCR collection completed at $(date)"
